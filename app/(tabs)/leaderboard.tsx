import { View, Text } from "react-native";
import React from "react";

const leaderboard = () => {
	return (
		<View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
			<Text>Leaderboard screen content</Text>
		</View>
	);
};

export default leaderboard;
