import { View, Text } from "react-native";
import React from "react";

const progress = () => {
	return (
		<View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
			<Text>Progress screen content</Text>
		</View>
	);
};

export default progress;
